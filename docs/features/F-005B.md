# F-005B: Encounter Card Gallery and Workspace Entry

## Objective

Replace the open-campaign map manager list with a visual encounter gallery and introduce the concept of opening an encounter workspace.

## User Value

The GM can browse prepared encounter maps visually, open one for prep, and deliberately decide when a map should appear on the player display.

## Non-Goals

- Do not add initiative tracking.
- Do not add notes.
- Do not add tokens.
- Do not add dynamic lighting.
- Do not add VTT-style left navigation.
- Do not add campaign members or characters.
- Do not add fog controls except for placeholders needed to prepare F-006.
- Do not rename storage or APIs without a reviewed migration plan.

## Acceptance Notes

- Open campaign page shows encounter/map cards with map thumbnails.
- Cards display the encounter/map name.
- Cards display simple status such as `Shown to Players` if that map is currently on the player display.
- The map thumbnail is visually prominent because the map is the primary artifact.
- GM can still add or upload maps.
- GM can still rename maps.
- GM can still reorder maps.
- Clicking an encounter/map card opens an in-page encounter workspace placeholder.
- Clicking an encounter/map card does not automatically change the player display.
- Showing a map to players is an explicit card-level `Show to Players` action.
- The player display changes only after the explicit show action.
- The current player-shown encounter is visually distinguished.
- The currently selected/editing encounter is visually distinguished separately from the player-shown encounter.
- The layout preserves the fantasy/parchment theme and remains usable on Chromebook.

## Terminology Notes

- Prefer "encounter" in user-facing UI for prepared map cards.
- Preserve existing storage compatibility where needed; current storage may still use `maps`.
- Distinguish the selected/editing encounter from the shown-to-players encounter.
- Opening an encounter for editing is not the same as showing it to players.
- Existing `activeMapId` storage/API terminology currently represents the player-shown map, not necessarily the GM-selected editing encounter.
- A future migration to clearer naming such as `shownMapId` may be considered, but this feature should not perform a risky storage migration without explicit scope.

## Feature Readiness Checklist

- Clarification complete.
- Acceptance notes updated.
- Architecture reviewed.
- Test and validation plan defined.
- UX and workflow reviewed.
- Non-goals confirmed.
- Ready for build.

## Clarification Notes

- Put the explicit `Show to Players` action on each encounter card for F-005B.
- Clicking an encounter card opens an in-page lightweight workspace placeholder; the full encounter workspace shell remains F-005C.
- Preserve existing `activeMapId` behavior when no encounter has been explicitly shown; in F-005B it continues to mean the player-shown map.
- Keep selected/editing encounter state GM-local in the browser for this feature.

## Harness Compliance

Harness Version: 1

## Review Scope Checklist

| Area | Applicability | Evidence |
| --- | --- | --- |
| Persistence/data integrity | Reviewed | F-005B does not migrate storage; existing `activeMapId` remains the player-shown map and selected/editing encounter state stays GM-local. |
| External inputs | Reviewed | Map names and uploaded image assets already cross validation boundaries; gallery rendering must keep using DOM APIs and existing GM-only asset URLs. |
| Security/trust boundaries | Reviewed | Player view remains read-only; only GM route controls expose rename, reorder, upload, and explicit `Show to Players`. |
| Error handling/observability | Reviewed | Existing upload, rename, reorder, and image-load error messages must remain visible after replacing the list with cards. |
| Module boundaries | Reviewed | Changes stay in GM browser state/view/events/styles and browser workflow tests unless a server behavior gap appears. |
| Dependencies | Not Applicable | F-005B should not add dependencies, frameworks, cloud services, or new asset tooling. |
| Browser behavior | Reviewed | Chromium coverage must verify card thumbnails, selected versus shown states, explicit show behavior, rename/reorder preservation, and responsive gallery layout. |
| Test realism | Reviewed | Tests should use multiple valid map images and verify player asset requests or visible player canvas state does not change on workspace open. |
| Resource cleanup | Reviewed | Browser tests continue to use isolated HTTPS servers, temporary campaign data roots, and existing deterministic cleanup. |

## SDLC Evidence

### Clarification

- Reviewer: human:user
- Completed: 2026-06-25
- Result: Pass
- Evidence: The user accepted card-level `Show to Players`, an in-page lightweight workspace placeholder for card clicks, and preserving existing `activeMapId` behavior when no new explicit-show state migration exists.

### Architecture Review

- Reviewer: /root/f005b_architecture
- Completed: 2026-06-25
- Result: Pass
- Evidence: Confirmed the proposed scope fits the roadmap and architecture if `activeMapId` remains strictly the player-shown encounter, selected/editing encounter state is GM-local and non-broadcast, card click does not call the show endpoint, and card-level `Show to Players` remains the explicit player-display mutation.

### Implementation Design

- Reviewer: /root
- Completed: 2026-06-25
- Result: Pass
- Evidence: Replace the open-campaign map list with encounter cards rendered from existing map metadata and GM asset URLs, add GM-local selected encounter state, wire card clicks to open a workspace placeholder without persistence or broadcasts, keep rename/reorder/upload actions on cards, and reuse the existing active-map endpoint only for explicit `Show to Players`.

### Test and Validation Design

- Reviewer: /root/f005b_tests
- Completed: 2026-06-25
- Result: Pass
- Evidence: Required Chromium coverage for the selected/editing versus shown-to-players split, including multiple maps, prominent thumbnails, shown status, no player change or player asset reload on card open, explicit `Show to Players` updates, preserved rename/reorder controls, nested card control boundaries, and final `npm run quality`.

### UX and Workflow Review

- Reviewer: /root/f005b_ux
- Completed: 2026-06-25
- Result: Pass
- Evidence: Approved a responsive encounter card grid with visible thumbnails, card-level `Show to Players`, separate card-click workspace entry, renamed shown-to-players language, GM-local selected encounter state, touch-friendly non-hover controls, and no fog, notes, tokens, initiative, or VTT navigation.

### Coding and TDD

- Reviewer: /root
- Completed: 2026-06-25
- Result: Pass
- Evidence: Added a failing Chromium workflow for encounter cards that open a workspace without changing the player display, confirmed it failed against the old map-list UI, then implemented GM-local selected encounter state, thumbnail encounter cards, selected and shown status pills, isolated card/button event paths, and explicit show behavior until the focused F-005B workflow and full Chromium suite passed.

### Code Review

- Reviewer: /root/f005b_code_review
- Completed: 2026-06-25
- Result: Pass
- Evidence: Reviewed the working diff and found no evidence that card selection changes the player display or bypasses the existing `activeMapId` show path. Identified a keyboard-accessibility gap for opening an encounter for prep; the implementation was updated with a real prep/open button and keyboard browser coverage.

### Docs and Tracker Review

- Reviewer: /root/f005b_docs
- Completed: 2026-06-25
- Result: Pass
- Evidence: Reviewed feature, tracker, acceptance, architecture, roadmap, README, and module-baseline changes. Confirmed acceptance, architecture, and module-baseline direction, and identified completion-language consistency work to resolve during final status reconciliation.

### Final-Diff Review

- Reviewer: /root/f005b_final_diff
- Completed: 2026-06-25
- Result: Pass
- Evidence: Reviewed the complete implementation, tests, docs, accessibility fix, findings table, and module-baseline updates. Confirmed the code and tests stay scoped to encounter cards, GM-local prep selection, explicit `Show to Players`, and keyboard-accessible prep opening. Identified the remaining status reconciliation item, which was fixed during final completion.

### Final Validation

- Reviewer: /root
- Completed: 2026-06-25
- Result: Pass
- Evidence: `npm run quality` passed lint, format check, 21-module baseline, harness compliance, 79 Node tests with coverage thresholds, 12 Chromium browser workflows, and high-severity dependency audit with zero vulnerabilities. Focused validation also passed the new F-005B Chromium workflow, full `npm run test:browser`, `npm test`, module baseline, format check, and harness check. Physical Chromebook/iPad validation was not repeated for F-005B.

## Material Findings

| ID | Stage | Reviewer | Severity | Finding | Recommendation | Disposition | Evidence | Approved By | Follow-up | Review By | Residual Risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F-005B-R001 | Code Review | /root/f005b_code_review | Medium | Opening an encounter for prep was mouse-only because cards and thumbnails were plain elements with click handling. | Make the prep/open affordance a real keyboard-focusable action and add keyboard coverage. | Fixed | Encounter thumbnails are now wrapped in `Open <name> for prep` buttons, event handling has a separate `select-encounter` action, and Chromium presses Enter on the prep button while confirming the player display and asset requests do not change. | N/A | N/A | N/A | None |
| F-005B-R002 | Docs and Tracker Review | /root/f005b_docs | Low | Roadmap and README described F-005B as complete while the governed feature record and tracker were still in Building. | Align feature doc, tracker, roadmap, and README status during final reconciliation. | Fixed | Final docs reconciliation moves all F-005B records to Done together after required reviews and validation, preserving README current-status language as shipped behavior. | N/A | N/A | N/A | None |
| F-005B-R003 | Final-Diff Review | /root/f005b_final_diff | Medium | The final-diff review found F-005B completion language still inconsistent with the feature record and tracker before final validation status was recorded. | Reconcile the feature doc, tracker, roadmap, README, and findings disposition before Done. | Fixed | Final validation evidence is recorded, the feature status is Done, the tracker row is Complete and Done, and README and roadmap completion language now matches the governed feature record. | N/A | N/A | N/A | None |

## Affected Systems

- GM view: open-campaign encounter gallery, thumbnail cards, workspace entry, explicit show action.
- Player view: updates only when the GM explicitly shows an encounter to players.
- Local server: may continue using existing active-map endpoints until a reviewed rename or migration is planned.
- Map/fog rendering: thumbnail display and existing canvas display paths.
- Storage: existing map metadata remains compatible.
- Documentation: update acceptance tests and terminology notes around selected/editing versus shown-to-players encounters.

## Validation Plan

Open a campaign with multiple maps, verify encounter cards with thumbnails, rename and reorder entries, open an encounter workspace placeholder without changing the player display, then use card-level `Show to Players` and confirm only that explicit action updates the player view. Run focused Chromium workflow coverage plus the repository quality command.

## Open Questions

- None for the planned F-005B scope.

## Status

Done.
