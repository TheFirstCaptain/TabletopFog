# F-011: Campaign Waiting Screen

## Objective

Replace the generic Player Display waiting state with a campaign-specific
waiting screen while a campaign is open and nothing is currently shown to
players, with optional GM-managed Campaign Image support.

## User Value

Always-on table displays can show campaign context and atmosphere between
encounters or handouts instead of a generic blank waiting message.

## Non-Goals

- Do not add slideshow mode.
- Do not add multiple campaign waiting images.
- Do not add text notes, campaign notes, or session notes.
- Do not add video, audio, PDF, or non-image waiting-screen media.
- Do not make opening or editing an encounter change the Player Display.
- Do not let the Player Display choose, upload, replace, remove, or mutate the
  campaign image.
- Do not add tokens, initiative, dice, character sheets, rules automation,
  dynamic lighting, cloud hosting, login, or authentication.

## Acceptance Notes

- The GM can upload, replace, and remove one Campaign Image from the
  create/edit campaign flow.
- The Campaign Image uses the same supported image inputs and validation
  expectations as map and handout upload unless implementation review finds a
  documented reason to differ.
- When no campaign is open, the Player Display keeps the current plain
  `Waiting for GM.` state.
- When a campaign is open and no encounter or handout is shown to players, the
  Player Display shows `Waiting for GM - <Campaign Name>`.
- If that campaign has a Campaign Image, the Player Display uses it as a
  background with the waiting text over it.
- If that campaign has no Campaign Image, the Player Display still shows
  `Waiting for GM - <Campaign Name>` without a background image.
- Showing an encounter or handout replaces the campaign waiting screen.
- Clearing the shown encounter or handout returns to the campaign waiting
  screen for the open campaign.
- Uploading, replacing, or removing the Campaign Image updates the Player
  Display live when players are currently on the campaign waiting screen.
- Uploading, replacing, or removing the Campaign Image does not change the
  Player Display while an encounter or handout is currently shown to players.
- The Player Display remains read-only.

## Child Features

- Complete: [F-011A](./F-011A.md), Campaign-Specific Waiting State.
- [F-011B](./F-011B.md): Campaign Image Management.
- [F-011C](./F-011C.md): Campaign Waiting Image Display.

## Feature Readiness Checklist

- Clarification complete.
- Acceptance notes updated.
- Architecture reviewed.
- Test and validation plan defined.
- UX and workflow reviewed.
- Non-goals confirmed.
- Ready for build.

## Clarification Notes

- On 2026-08-01, the user proposed replacing the generic `Waiting for GM`
  Player Display state with a campaign-specific screen saver for always-on
  displays.
- The Campaign Image should be set only from the create/edit campaign flow.
- The GM must be able to remove the Campaign Image, not only replace it.
- If no Campaign Image is set while a campaign is open, the Player Display
  should show `Waiting for GM - <Campaign Name>`.
- If a Campaign Image is set, the Player Display should render it like a
  background with the waiting text over it.
- Campaign Image changes should update live when players are currently on that
  waiting screen.
- On 2026-08-01, the user approved splitting the feature into smaller child
  features for the campaign-specific waiting state, Campaign Image management,
  and Campaign Image Player Display rendering.

## SDLC Tier

Tier: 0 for this planning split.

Rationale: This update only splits an existing proposed feature into smaller
planning and tracker records. The implementation child features require their
own tier classification before leaving `Proposed`; current planning expects
Tier 3 because the slices touch shared campaign state, image input validation,
filesystem writes, Socket.IO sync, player projection, Player Display rendering,
and the player read-only boundary.

## Harness Compliance

Harness Version: 1

## Review Scope Checklist

| Area | Applicability | Evidence |
| --- | --- | --- |
| Persistence/data integrity | Reviewed | Planning isolates Campaign Image storage and file-integrity work in F-011B. No storage behavior changes in this planning update. |
| External inputs | Reviewed | Planning keeps Campaign Image inputs aligned with map and handout image validation and defers implementation detail to F-011B. |
| Security/trust boundaries | Reviewed | Planning preserves GM-only Campaign Image mutation and read-only Player Display boundaries. No route behavior changes in this planning update. |
| Error handling/observability | Reviewed | Planning defers missing asset, invalid image, replacement rollback, and recovery behavior to F-011B and F-011C. |
| Module boundaries | Reviewed | Planning splits shared waiting state, Campaign Image storage, and Player Display image rendering into separate child features. |
| Dependencies | Not Applicable | No dependency changes are currently expected. |
| Browser behavior | Reviewed | Planning identifies Player Display waiting-state and background-rendering behavior for child-feature validation. |
| Test realism | Reviewed | Planning calls for realistic campaign state, shown-target, valid image, invalid image, mismatched input, removal, replacement, and recovery coverage during implementation. |
| Resource cleanup | Reviewed | Planning expects child browser tests to use existing isolated HTTPS servers, temporary data roots, generated certificates, sockets, and browser cleanup. |

## SDLC Evidence

### Clarification

- Reviewer: human:user
- Completed: 2026-08-01
- Result: Pass
- Evidence: User clarified that the Campaign Image is configured only from
  create/edit campaign, must support removal, falls back to
  `Waiting for GM - <Campaign Name>` when absent, renders as a background with
  text over it when present, and updates live while the Player Display is on
  the campaign waiting screen. User then approved splitting the feature into
  three child records.

### Architecture Review

- Reviewer: /root
- Completed: 2026-08-01
- Result: Pass
- Evidence: Planning split keeps the umbrella as documentation-only and assigns
  shared waiting state, Campaign Image storage, and Player Display background
  rendering to independently reviewable child features.

### Implementation Design

- Reviewer: /root
- Completed: 2026-08-01
- Result: Pass
- Evidence: Planned F-011A for campaign-specific waiting copy and shown-target
  transitions, F-011B for Campaign Image upload/replace/remove storage, and
  F-011C for image-backed Player Display rendering and live updates.

### Test and Validation Design

- Reviewer: /root
- Completed: 2026-08-01
- Result: Pass
- Evidence: Planning assigns focused Node and Chromium validation to child
  features and keeps full quality for later code or tooling changes.

### UX and Workflow Review

- Reviewer: /root
- Completed: 2026-08-01
- Result: Pass
- Evidence: Planning preserves the explicit `Show to Players` boundary:
  campaign waiting screens appear only when no encounter or handout is shown,
  and Player Display remains read-only.

### Coding and TDD

- Reviewer: /root
- Completed: 2026-08-01
- Result: Pass
- Evidence: No application code, tests, storage, API, or shipped behavior
  changed. TDD is not applicable to this planning-only split.

### Code Review

- Reviewer: /root
- Completed: 2026-08-01
- Result: Pass
- Evidence: Main-agent documentation diff review found the split scoped to
  feature planning records and no code paths.

### Docs and Tracker Review

- Reviewer: /root
- Completed: 2026-08-01
- Result: Pass
- Evidence: Updated F-011, added F-011A through F-011C, updated the feature
  tracker, roadmap, and acceptance-test section heading. README remained
  accurate because no setup, command, current-status, or shipped workflow
  behavior changed.

### Final-Diff Review

- Reviewer: /root
- Completed: 2026-08-01
- Result: Pass
- Evidence: Reviewed the documentation-only diff for F-011, F-011A through
  F-011C, the feature tracker, roadmap, and acceptance-test heading; no code,
  tests, storage, API, command, or shipped behavior changes were included.

### Final Validation

- Reviewer: /root
- Completed: 2026-08-01
- Result: Pass
- Evidence: `git diff --check` and `npm run harness:check` passed. Full
  `npm run quality` is not required because this is a planning-only
  documentation update with no code, tests, tooling, command, storage, API, or
  shipped behavior changes.

## Material Findings

No material findings.

## Affected Systems

- GM view: future child features cover create/edit campaign flow for Campaign
  Image upload, replace, and remove controls.
- Player view: future child features cover campaign-specific waiting screen
  copy and optional background image rendering.
- Local server: future child features cover campaign image upload, removal,
  asset serving, shared-state projection, and live update behavior.
- Map/fog rendering: future child features must preserve encounter and handout
  replacement behavior without changing map, fog, zoom, pan, fullscreen, or
  handout rotation behavior.
- Storage: future child features cover campaign metadata and one copied
  Campaign Image asset.
- Documentation: this planning split updates feature tracker, roadmap,
  acceptance tests, and feature records.

## Validation Plan

Expected validation for the child features includes focused Node tests for
campaign waiting-state projection, campaign image storage, input validation,
removal, recovery, and projection behavior; Chromium GM/Player Display workflow
coverage for waiting-screen fallback, background rendering, live update
behavior, and replacement by encounters or handouts; and full
`npm run quality` before implementation completion. Manual iPad or always-on
display validation should be recorded if practical.

## Open Questions

- None currently.

## Status

Proposed.
