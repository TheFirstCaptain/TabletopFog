# F-002: Local Connectivity Spike

## Objective

Prove that the GM machine can host TabletopFog locally and an iPad can connect over the same Wi-Fi network.

## User Value

This validates the core MVP assumption before any map, fog, or storage complexity is added.

## Non-Goals

- Do not add map loading.
- Do not add fog rendering.
- Do not add save/load.
- Do not add auth, cloud hosting, tokens, dice, sheets, or combat automation.

## Acceptance Notes

- GM page is served from a local server.
- Player page is served from the same local server.
- iPad can connect using the GM machine's LAN IP address.
- GM can trigger one simple state change.
- Player view updates live without manual refresh.
- The same workflow is tested on home Wi-Fi and a friend's Wi-Fi.

## Affected Systems

- GM view: simple control for one state change.
- Player view: read-only display of synced state.
- Local server: serves both pages and broadcasts state.
- Map/fog rendering: none.
- Storage: in-memory only.
- Documentation: update acceptance tests with actual commands and network notes.

## Validation Plan

Run the future local server command, open the GM page locally, open the player page from an iPad via LAN IP, trigger a state change, and confirm the player view updates.

## Open Questions

- Use Socket.IO or plain WebSocket for the first spike?
- Which port should be the documented default?
- What Chromebook runtime setup should be assumed?

## Status

Proposed.
