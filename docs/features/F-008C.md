# F-008C: Undo Fog Action

## Objective

Let the GM undo the most recent fog operation for the selected encounter.

## User Value

The GM can recover from an accidental hide, reveal, or clear during prep or play
without rebuilding fog manually or reloading campaign data.

## Non-Goals

- Do not add brush tools, circles, polygons, shape editing, or per-shape
  selection.
- Do not add redo.
- Do not add multi-user conflict handling.
- Do not add player controls.
- Do not change which encounter is `Shown to Players`.
- Do not add dynamic lighting, tokens, vision cones, notes, initiative, or other
  VTT features.
- Do not migrate storage/API terminology such as `maps`, `activeMapId`, or
  `fog`.

## Acceptance Notes

- Undo controls live in the Encounter Workspace Fog tools group.
- Undo is a compact text button labeled `Undo`.
- Undo affects only the selected encounter.
- Undo is disabled when the selected encounter has no undoable fog change.
- Undo can be clicked repeatedly; each click undoes the latest undoable fog
  change for the selected encounter.
- Undoing a Hide or Reveal operation removes the latest fog operation for the
  selected encounter.
- Undoing Clear Fog restores the selected encounter's prior ordered fog
  operations.
- Clear Fog remains confirmed before it clears fog.
- Clear Fog confirmation copy must not say the action cannot be undone; it
  should explain that Undo is available until the campaign is reloaded.
- Successful undo may show quiet GM-local status text such as `Fog undone.`
- Undo respects the selected/editing versus `Shown to Players` distinction.
- If the selected encounter is currently `Shown to Players`, undo updates the
  Player Display live.
- If the selected encounter is not `Shown to Players`, undo does not change the
  Player Display.
- Undo autosaves the resulting fog state to local campaign storage.
- Undo history is runtime-only and does not survive campaign reloads or server
  restarts.
- Invalid or rejected undo requests leave runtime state, campaign storage, and
  Player Display unchanged.
- Failed undo shows quiet GM-local status text such as `Could not undo fog.
  Nothing changed.`
- Player Display remains read-only.

## Clarification Notes

- Scaffolded on 2026-07-04 as the next proposed F-008 quality-of-life slice.
- User confirmed on 2026-07-04 that undo should be single-step but repeatable:
  each click undoes the current latest undoable fog change.
- User confirmed on 2026-07-04 that Clear Fog should be undoable by restoring
  the selected encounter's prior ordered fog operation list.
- User confirmed on 2026-07-04 that undo history should not survive campaign
  reloads; only the resulting fog state persists.
- User confirmed on 2026-07-04 that Undo should be a compact text button in the
  Fog tools group.
- User confirmed on 2026-07-04 that undo failures should use quiet GM-local
  status text.
- Because fog now persists to `campaign.json`, undo must define how it interacts
  with autosave before implementation begins. The clarified behavior is that
  each successful undo autosaves the resulting fog state, while the undo history
  itself remains runtime-only.

## Harness Compliance

Harness Version: 1

## Feature Readiness Checklist

- Clarification complete.
- Acceptance notes updated.
- Architecture reviewed.
- Test and validation plan defined.
- UX and workflow reviewed.
- Non-goals confirmed.
- Ready for build.

## Review Scope Checklist

| Area | Applicability | Evidence |
| --- | --- | --- |
| Persistence/data integrity | Reviewed | Architecture and implementation reviews require runtime-only undo stacks, prior ordered fog snapshots, autosaved resulting fog lists, and atomic rejected undo behavior. |
| External inputs | Reviewed | Test review requires GM-only undo route coverage for no-history, missing target, stale target, player-originated request, and malformed request cases. |
| Security/trust boundaries | Reviewed | Architecture review requires the undo route to use `requireGm` and add no Player Display controls or player mutation route. |
| Error handling/observability | Reviewed | UX and test reviews require disabled unavailable undo, quiet failed-undo status, and unchanged runtime state, storage, and projection after rejected undo. |
| Module boundaries | Reviewed | Implementation review scopes runtime undo state to server state, route/API/controller/view/event wiring to existing fog boundaries, and no storage/schema serialization. |
| Dependencies | Not Applicable | F-008C adds no dependencies. |
| Browser behavior | Reviewed | UX and test reviews require stable compact `Undo` button behavior, repeated keyboard-accessible clicks, shown/unshown projection semantics, and updated Clear Fog confirmation copy. |
| Test realism | Reviewed | Test review requires representative valid, invalid, missing, stale, player-role, storage-failure, reload, shown, and unshown cases. |
| Resource cleanup | Reviewed | Test review requires existing temporary directory helpers, Socket.IO client/server cleanup in `finally`, and scoped Playwright dialogs/routes. |

## SDLC Tier

Tier: 3.

Rationale: F-008C is expected to touch fog persistence, filesystem writes,
server state, Socket.IO synchronization, Player Display projection, player
read-only guarantees, and map/fog rendering semantics. Those areas are Tier 3
under `docs/decisions/decision-004-mandatory-subagent-sdlc.md`.

## SDLC Evidence

### Clarification

- Reviewer: /root
- Completed: 2026-07-04
- Result: Pass
- Evidence: User confirmed repeatable single-step undo, undoable Clear Fog,
  runtime-only undo history, compact `Undo` button placement, disabled
  unavailable undo, and quiet GM-local failure text.

### Architecture Review

- Reviewer: /root/architecture_review
- Completed: 2026-07-04
- Result: Pass
- Evidence: Recommended a GM-only `POST
  /api/campaigns/:campaignId/maps/:mapId/fog-operations/undo` route, server
  runtime undo stacks keyed by campaign and encounter, prior ordered fog lists
  rather than inverse operations, autosaving through `campaignStorage.setMapFog`,
  preserving `activeMapId`, and continuing player projection through the
  existing role projection path.

### Implementation Design

- Reviewer: /root/implementation_design_review
- Completed: 2026-07-04
- Result: Pass
- Evidence: Recommended server-owned runtime undo stacks storing
  `previousFogOperations`, recording pre-change lists for append and Clear Fog,
  popping only after successful persistence, adding server-derived undo
  availability for the GM UI, and keeping undo metadata out of `campaign.json`.

### Test and Validation Design

- Reviewer: /root/test_validation_review
- Completed: 2026-07-04
- Result: Pass
- Evidence: Required Node coverage for state undo stacks, server GM-only undo
  route, persistence, reload clearing history, shown/unshown projection, and
  rejected-operation atomicity. Required Chromium coverage for the `Undo`
  button, Hide/Reveal/Clear Fog undo, repeated undo, unshown isolation, and
  Player Display read-only behavior.

### UX and Workflow Review

- Reviewer: /root/ux_workflow_review
- Completed: 2026-07-04
- Result: Pass
- Evidence: Recommended compact `Undo` placement in the Fog tools group,
  stable disabled state, quiet `Fog undone.` and `Could not undo fog. Nothing
  changed.` status messages, focus remaining on Undo after use, no modal for
  undo, and Clear Fog confirmation copy that explains Undo is available until
  campaign reload.

### Coding and TDD

- Reviewer: /root
- Completed: 2026-07-04
- Result: Pass
- Evidence: Added failing state-store tests for per-encounter undo stacks,
  Clear Fog restoration, invalid undo rejection, and reload clearing history;
  implemented server runtime undo history with prior ordered fog snapshots and
  server-derived `canUndoFogOperation`; added GM-only undo route and UI wiring;
  added focused server and Chromium coverage for shown/unshown undo,
  persistence, rejected undo, storage-failure atomicity, stale/deleted targets,
  malformed request handling, quiet failure messaging, and Player Display
  read-only behavior. Focused `node --test test/state.test.js`,
  `node --test test/server.test.js`, `npx playwright test
  browser-test/workflows.spec.js -g "GM (Clear Fog|Undo fog)"`, and follow-up
  `npx playwright test browser-test/workflows.spec.js -g "GM Undo fog"` passed.

### Code Review

- Reviewer: /root/code_review
- Completed: 2026-07-04
- Result: Pass
- Evidence: Initial code review found missing storage-failure atomicity,
  malformed request, stale target, and quiet failure UI coverage. Follow-up
  review passed with no material findings after adding server tests for
  storage-failure atomicity, malformed undo requests, deleted-map stale undo
  rejection, preserving undo history across normal mutations, and Chromium
  coverage for quiet failed-undo messaging.

### Docs and Tracker Review

- Reviewer: /root/docs_tracker_review
- Completed: 2026-07-04
- Result: Pass
- Evidence: Initial docs/tracker review found status mismatches, open planning
  findings, stale parent tracker notes, and missing failed-undo acceptance-test
  language. The docs sweep updated acceptance tests for F-008C, README current
  status, F-008 umbrella status, roadmap status, tracker notes, F-008C evidence,
  and findings dispositions. Final status alignment is completed before final
  validation.

### Final-Diff Review

- Reviewer: /root/final_diff_review
- Completed: 2026-07-04
- Result: Pass
- Evidence: Final-diff review found no material findings. The review covered
  state/storage ordering, GM-only undo route, Player Display projection and
  read-only behavior, selected versus `Shown to Players` semantics,
  docs/tracker consistency, and prior review-finding disposition. Reviewer
  validation passed `node --test test/state.test.js`, `node --test
  test/server.test.js`, and `npx playwright test browser-test/workflows.spec.js
  -g "GM (Clear Fog|Undo fog)"`.

### Final Validation

- Reviewer: /root
- Completed: 2026-07-04
- Result: Pass
- Evidence: `npm run quality` passed. The quality gate included lint,
  Prettier format check, module baseline, harness compliance for 44 tracked
  features, Node coverage with 121 passing tests, Chromium browser workflows
  with 29 passing tests, and high-severity dependency audit with zero
  vulnerabilities. Focused validation also passed during implementation:
  `node --test test/state.test.js`, `node --test test/server.test.js`, `npm
  test`, `npm run test:browser`, and F-008C-specific Chromium checks.

## Affected Systems

- GM view: adds an Undo fog control in the Encounter Workspace and quiet
  success/failure status text.
- Player view: receives live fog updates only when the selected encounter is
  also `Shown to Players`.
- Local server: adds a GM-only undo mutation for selected encounter fog state.
- Map/fog rendering: re-renders the ordered fog operation list after undo.
- Storage: autosaves the resulting fog state in local campaign storage while
  keeping undo history runtime-only.
- Documentation: acceptance tests, README current status, roadmap, feature
  tracker, and F-008 umbrella notes.

## Validation Plan

Automated Node coverage should include state-store undo stack behavior per
encounter; undoing Hide; undoing Reveal; repeated undo; Clear Fog restoring the
prior ordered fog operation list; selected-unshown isolation; shown-player live
updates; autosave persistence; no-history and missing-target rejection; player
role rejection; storage-failure atomicity; and reload/server restart clearing
undo history while preserving the resulting fog state.

Automated Chromium coverage should include `Undo` appearing disabled in the
Encounter Workspace Fog tools group; Hide then Undo; Reveal then Undo; Clear Fog
confirmation and undo restoration; repeated undo clicks; selected unshown undo
leaving the Player Display unchanged; quiet failure messaging; and Player
Display remaining read-only.

Run focused Node and Chromium tests for the changed workflow, then run
`npm run harness:check`, `npm run test:browser`, and `npm run quality` before
marking the feature complete.

## Material Findings

| ID | Stage | Reviewer | Severity | Finding | Recommendation | Disposition | Evidence | Approved By | Follow-up | Review By | Residual Risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F-008C-R001 | Architecture Review | /root/architecture_review | High | Undo history can be lost if it is cleared on every `stateStore.setCampaign` call after fog persistence. | Preserve undo history across normal fog mutation hydration while clearing it on campaign load, reload, server restart, and deleted-map pruning. | Fixed | `preserveFogUndo` keeps history across normal mutations; campaign open/reload clears history; deleted-map pruning removes stale history. Covered by `test/state.test.js`, `test/server.test.js`, and follow-up code review. | N/A | N/A | N/A | None |
| F-008C-R002 | Architecture Review | /root/architecture_review | High | Clear Fog cannot be undone by only removing the latest fog operation. | Store prior ordered fog lists as undo entries so Clear Fog can restore the full previous list. | Fixed | Runtime undo entries store prior ordered fog lists; Clear Fog undo restoration is covered by state, server, and Chromium tests. | N/A | N/A | N/A | None |
| F-008C-R003 | Architecture Review | /root/architecture_review | Medium | Invalid undo must be atomic across memory, storage, and Socket.IO. | Do not pop history, mutate state, or emit sync until persistence succeeds. | Fixed | Undo route peeks the next undo state, persists first, then consumes history. Storage-failure atomicity is covered by server tests and follow-up code review. | N/A | N/A | N/A | None |
| F-008C-R004 | Implementation Design | /root/implementation_design_review | Medium | Undo availability cannot be inferred from current fog count because Clear Fog leaves zero fog with an undo entry. | Add server-derived runtime undo availability for GM UI rendering. | Fixed | GM campaign state includes runtime-only `canUndoFogOperation`; browser coverage verifies Undo remains enabled after Clear Fog. | N/A | N/A | N/A | None |
| F-008C-R005 | UX and Workflow Review | /root/ux_workflow_review | High | Current Clear Fog confirmation says `This can't be undone.` | Update confirmation copy to explain Undo remains available until campaign reload. | Fixed | Clear Fog confirmation now says `You can use Undo until this campaign is reloaded.` Browser coverage asserts the updated copy. | N/A | N/A | N/A | None |
| F-008C-R006 | UX and Workflow Review | /root/ux_workflow_review | Medium | Runtime-only undo history needs a visible boundary for Clear Fog. | Mention Undo is available until campaign reload in Clear Fog confirmation. | Fixed | Clear Fog confirmation and acceptance tests document the runtime-only boundary. | N/A | N/A | N/A | None |
| F-008C-R007 | Test and Validation Design | /root/test_validation_review | High | F-008C behavior has no implementation or tests yet. | Add focused Node and Chromium coverage before completion. | Fixed | Added state, server, and Chromium tests for undo stack behavior, GM route, persistence, projection, reload, rejected undo, storage failure, stale target, malformed request, quiet failure text, and read-only player behavior. | N/A | N/A | N/A | None |
| F-008C-R008 | Test and Validation Design | /root/test_validation_review | Medium | Clear Fog copy conflicts with undoable Clear Fog. | Update copy and add browser assertion. | Fixed | Updated Clear Fog confirmation and added browser assertions for shown and unshown Clear Fog confirmation copy. | N/A | N/A | N/A | None |

## Open Questions

- Resolved: V1 undo is single-step but repeatable.
- Resolved: Clear Fog is undoable in this slice.
- Resolved: undo history does not survive campaign reloads or server restarts;
  only the resulting fog state persists.
- Resolved: Undo is a compact text button in the Fog tools group.
- Resolved: unavailable undo is disabled, and failed undo uses quiet GM-local
  status text.

## Status

Done.
