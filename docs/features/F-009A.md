# F-009A: Local Quality Command and Baseline Enforcement

## Objective

Provide one documented local command that runs the repository's required
automated quality checks with deterministic pass/fail behavior.

## User Value

Maintainers and agents can verify a change using one repeatable command instead
of relying on memory or prose-only completion claims.

## Non-Goals

- Do not add continuous integration; F-009C owns remote enforcement.
- Do not refactor application modules to satisfy new checks.
- Do not introduce arbitrary coverage or file-size targets without baselines.
- Do not add a frontend application build system.

## Acceptance Notes

- `npm run quality` or an equivalently clear command is the authoritative local
  validation entry point.
- The command runs automated tests, linting, formatting checks, dependency
  auditing, coverage measurement/regression policy, and module-growth checks.
- Failures identify the failed check and return a non-zero status.
- Dependency auditing uses a documented blocking severity and exception policy.
- Coverage and module checks use recorded baselines and ratchet against
  regression.
- Existing documented development commands remain available.

## Feature Readiness Checklist

- Clarification incomplete.
- Acceptance notes drafted.
- Architecture review pending.
- Test and validation plan drafted.
- UX and workflow review pending for contributor output.
- Non-goals confirmed.
- Not ready for build.

## Clarification Notes

- E-005 must record the module baseline before file-growth enforcement is set.
- Select linting and formatting tools that work with CommonJS and plain browser
  JavaScript without adding an application build step.
- Record current coverage before defining a no-regression policy.
- Define how network-dependent dependency auditing behaves when offline.

## SDLC Agent Outputs

### Architecture Review

- Pending.

### Implementation Design

- Pending.

### Test and Validation Design

- Validate each quality subcommand independently and through the aggregate
  command, including a known failing case where practical.

### UX and Workflow Review

- Ensure output is concise and identifies corrective action.

### Coding Notes

- No implementation has started.

### Code Review

- Pending.

### Docs and Tracker Review

- Initial plan recorded on 2026-06-20.

## Affected Systems

- Tests and tooling: package scripts, linting, formatting, audit, coverage, and
  module checks.
- Documentation: README, development workflow, Definition of Done, AGENTS, and
  AI harness after implementation.

## Validation Plan

Run the aggregate command from a clean installation, demonstrate that each
blocking check can fail the aggregate command, and confirm existing development
commands continue to work.

## Dependencies

- [E-005](../engineering/E-005.md): module responsibility and growth baselines.

## Open Questions

- Which linting and formatting tools should be used?
- What audit severity blocks completion?
- What coverage regression policy is appropriate for the initial baseline?

## Status

Proposed.
