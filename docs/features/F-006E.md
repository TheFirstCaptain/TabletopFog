# F-006E: Clear Fog

## Objective

Add an explicit GM control to clear all fog operations from the selected
encounter.

## User Value

The GM can recover quickly from prep mistakes or reset a map without deleting
and recreating the encounter.

## Non-Goals

- Do not add undo history or per-operation deletion.
- Do not add reset-to-saved-state behavior.
- Do not persist fog to campaign storage.
- Do not add player controls.
- Do not add dynamic lighting, tokens, vision cones, notes, or initiative.

## Acceptance Notes

- Clear Fog is always visible in the Encounter Workspace Fog tools group.
- Clear Fog is disabled when the selected encounter has no fog operations.
- Clear Fog removes all in-memory fog operations for the selected encounter.
- Clear Fog is explicit and confirmed before removing fog.
- If the selected encounter is shown to players, clearing fog updates the Player
  Display live.
- If the selected encounter is not shown to players, clearing fog does not
  change the Player Display.
- Clearing fog does not change which encounter is shown to players.
- Clearing fog does not require the selected map image to load successfully.
- Player Display remains read-only.

## Feature Readiness Checklist

- Clarification complete.
- Acceptance notes updated.
- Architecture reviewed.
- Test and validation plan defined.
- UX and workflow reviewed.
- Non-goals confirmed.
- Ready for build.

## Clarification Notes

- 2026-07-01: User confirmed they want a Clear Fog control as part of the F-006
  child-feature sequence.
- 2026-07-02: User chose always visible for Clear Fog, enabled only when the
  selected encounter has fog.

## Harness Compliance

Harness Version: 1

Tier: 3

Tier Rationale: F-006E changes server-side fog mutation, Socket.IO
synchronization, Player Display projection behavior, map/fog rendering state,
and GM destructive workflow behavior. Those areas match Decision 004's Tier 3
criteria.

## Review Scope Checklist

| Area | Applicability | Evidence |
| --- | --- | --- |
| Persistence/data integrity | Reviewed | Clear Fog deletes only the selected encounter's in-memory fog operations and does not write fog state to `campaign.json`. |
| External inputs | Reviewed | The clear route validates the GM role, campaign id, and encounter id before mutating state. |
| Security/trust boundaries | Reviewed | The clear route is GM-only, Player Display remains read-only, and player projection still exposes only the shown-to-players encounter. |
| Error handling/observability | Reviewed | Missing targets and player-originated clear requests are rejected without mutating fog state; GM failures surface through the existing campaign message path. |
| Module boundaries | Reviewed | State mutation lives in `server/state.js`, HTTP routing in `server/index.js`, GM API/controller/view/events in `public/`, and rendering remains in `public/map-canvas.js`. |
| Dependencies | Not Applicable | No runtime or development dependency changes are part of F-006E. |
| Browser behavior | Reviewed | Chromium coverage exercises visible disabled Clear Fog, cancel, confirmed shown clear, unshown isolation, failed-image clearing, read-only Player Display, and no persistence. |
| Test realism | Reviewed | Tests use real uploaded PNG maps, Socket.IO sync, GM HTTP requests, browser confirmation dialogs, asset-load failures, and isolated campaign data roots. |
| Resource cleanup | Reviewed | The feature adds no new files, sockets, timers, routes, or processes beyond existing isolated test fixtures. |

## SDLC Evidence

### Clarification

- Reviewer: human:user
- Completed: 2026-07-02
- Result: Pass
- Evidence: User answered the open question by choosing an always-visible Clear Fog control that is enabled only when the selected encounter has fog.

### Architecture Review

- Reviewer: /root/architecture_review
- Completed: 2026-07-02
- Result: Pass
- Evidence: Confirmed F-006E fits MVP boundaries if implemented as a GM-only, confirmed, server-side clear mutation for the selected encounter's in-memory fog list while preserving Player Display projection filtering, no persistence, no undo, no player controls, and no active-map change.

### Implementation Design

- Reviewer: /root/implementation_design
- Completed: 2026-07-02
- Result: Pass
- Evidence: Reviewed the working-tree design and confirmed the server-side targeted clear data flow avoids stale client-side replacement, preserves `activeMapId`, and keeps player projection scoped to the shown encounter. Identified and fixed the edge case where Clear Fog was disabled when a fogged map image failed to load.

### Test and Validation Design

- Reviewer: /root/test_validation_design
- Completed: 2026-07-02
- Result: Pass
- Evidence: Recommended Node state and server tests for selected encounter clear, GM-only access, invalid targets, no persistence, and player projection, plus Chromium coverage for visible disabled Clear Fog, cancel, confirmed shown clear, unshown isolation, read-only Player Display, and no persistence.

### UX and Workflow Review

- Reviewer: /root/ux_workflow_review
- Completed: 2026-07-02
- Result: Pass
- Evidence: Confirmed always-visible disabled Clear Fog supports a standing GM, avoids layout shift, stays compact in the Fog group, requires confirmation for Never Surprise the GM, and does not create VTT scope creep when limited to a single destructive recovery action.

### Coding and TDD

- Reviewer: /root
- Completed: 2026-07-02
- Result: Pass
- Evidence: Added failing state, server, and Chromium Clear Fog coverage, then implemented `clearFogOperations`, a GM-only DELETE route, GM API/controller/event/view wiring, confirmation copy, disabled-state behavior based on selected encounter fog count, and a failed-image Clear Fog regression. Focused Node fog tests, focused Chromium Clear Fog/image-load workflows, and module baseline check pass.

### Code Review

- Reviewer: /root/code_review
- Completed: 2026-07-02
- Result: Pass
- Evidence: Reviewed the current working-tree diff for server/state mutation, GM wiring, player projection, persistence boundaries, tests, module baseline, README, acceptance tests, tracker, and F-006E docs. Confirmed the implementation is sound and found one low roadmap status inconsistency that was fixed.

### Docs and Tracker Review

- Reviewer: /root/docs_tracker_review
- Completed: 2026-07-02
- Result: Pass
- Evidence: Reviewed F-006E evidence, tracker, roadmap, acceptance tests, README, material findings, no-goals, and follow-up classification. Confirmed F-006E is correctly active at Building and found two low documentation gaps, both fixed before final diff review.

### Final-Diff Review

- Reviewer: /root/final_diff_review
- Completed: 2026-07-02
- Result: Pass
- Evidence: Reviewed the complete working-tree diff after implementation, tests, docs, tracker, module baseline, and review-finding fixes. Confirmed the diff stays scoped to F-006E, preserves GM-only mutation, selected-encounter scope, player projection filtering, no persistence, read-only Player Display, and unchanged shown encounter, with no new material findings.

### Final Validation

- Reviewer: /root
- Completed: 2026-07-02
- Result: Pass
- Evidence: Ran focused state/server fog tests, focused Chromium Clear Fog and image-load workflows, `npm test`, `npm run test:browser`, `npm run harness:check`, `npm run modules:check`, `git diff --check`, and `npm run quality`. The full quality gate passed lint, format check, module baseline, harness compliance, Node coverage tests, Chromium browser workflows, and high-severity dependency audit.

## Material Findings

| ID | Stage | Reviewer | Severity | Finding | Recommendation | Disposition | Evidence | Approved By | Follow-up | Review By | Residual Risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F-006E-R001 | Implementation Design | /root/implementation_design | Medium | Clear Fog was disabled when the selected map image was not ready, even if the selected encounter had fog operations. This could block recovery from fog state after an image-load failure. | Enable Clear Fog from selected encounter fog count rather than map image readiness while keeping Hide and Reveal tied to a ready map. | Fixed | `public/gm-view.js` now disables Clear Fog only when the selected encounter fog count is zero, and `browser-test/workflows.spec.js` verifies Clear Fog remains enabled and clears fog when the map image fails to load. | N/A | N/A | N/A | Low. Clear Fog remains available for fogged encounters even when drawing tools are disabled by image readiness. |
| F-006E-R002 | Code Review | /root/code_review | Low | `docs/roadmap.md` still listed F-006E as Proposed after implementation and tracker updates moved it to Building. | Update the roadmap status line before final docs/tracker review. | Fixed | `docs/roadmap.md` now lists F-006E as Building in the Milestone 4 sequence. | N/A | N/A | N/A | Low. Final docs/tracker review and harness validation will re-check status consistency. |
| F-006E-R003 | Docs and Tracker Review | /root/docs_tracker_review | Low | `docs/acceptance-tests.md` covered Clear Fog cancellation, shown and unshown projection, read-only player behavior, and no persistence, but omitted the requirement that Clear Fog still works when the selected map image fails to load. | Add a human-readable acceptance step and expected result for clearing fog after map image load failure. | Fixed | `docs/acceptance-tests.md` now includes failed-image Clear Fog steps and expected behavior. | N/A | N/A | N/A | Low. Browser coverage also verifies this failed-image case. |
| F-006E-R004 | Docs and Tracker Review | /root/docs_tracker_review | Low | README's planned technical direction still said future fog rendering even though current F-006 work renders fog through canvas. | Update README to describe current map and fog canvas rendering. | Fixed | README now says HTML Canvas is used for current map and fog rendering. | N/A | N/A | N/A | Low. README current-status and technical-direction sections now match implemented behavior. |

## Affected Systems

- GM view: add confirmed Clear Fog action.
- Player view: receive live cleared-fog state only for the shown encounter.
- Local server: clear selected encounter's in-memory fog operation list.
- Map/fog rendering: render no fog when the list is empty.
- Storage: no persistent storage changes.
- Documentation: update acceptance tests, README, tracker, and F-006 completion
  notes.

## Validation Plan

Create fog on the selected encounter, use Clear Fog, confirm the GM overlay is
removed, and confirm the Player Display updates only when that encounter is
shown to players. Confirm cancellation leaves fog unchanged. Confirm Clear Fog
is visible but disabled before fog exists, remains available when a fogged map
image fails to load, and does not persist fog state to `campaign.json`. Run
focused Node fog tests, focused Chromium Clear Fog/image-load workflows,
`npm test`, `npm run test:browser`, harness compliance, and the full
`npm run quality` before marking F-006E complete.

## Open Questions

- Resolved: Clear Fog is always visible in the Fog tools group and enabled only
  after the selected encounter has fog operations.

## Status

Done.
