# F-008E: Drag-To-Size Circle Fog Shape

## Objective

Add a true `Circle` fog shape that works like `Rectangle`: the GM presses on
the map, drags to choose the circle size, previews the shape during the drag,
and releases to commit one circular hide or reveal operation.

## User Value

The GM can quickly cover round rooms, towers, cave chambers, spell areas, and
other circular map regions without estimating a fixed brush size first.

## Non-Goals

- Do not replace Rectangle.
- Do not remove Brush.
- Do not add polygon, lasso, snap, measurement, dynamic lighting, tokens, line
  of sight, or automation.
- Do not change Player Display read-only behavior.
- Do not change which encounter is `Shown to Players`.
- Do not migrate storage/API terminology such as `maps`, `activeMapId`, or
  `fog`.

## Acceptance Notes

- Fog tools include `Rectangle`, `Brush`, and `Circle` as separate choices.
- `Rectangle` remains drag-to-size rectangular fog.
- `Brush` remains fixed-size freehand circular painting.
- `Circle` is drag-to-size and commits one circular operation per completed
  drag.
- Hide and Reveal apply to the active tool.
- The Circle preview is visible during drag and uses the same hide/reveal
  visual treatment as other drafts.
- Tiny drags do not create fog operations.
- Escape during an in-progress circle drag cancels the draft without changing
  fog.
- The committed circle uses normalized map-relative center coordinates and a
  radius normalized against the shorter map side.
- Circle operations persist as existing ordered `hide-circle` and
  `reveal-circle` operations.
- Circle changes respect selected/editing versus `Shown to Players`
  boundaries.
- Player Display remains read-only.

## Feature Readiness Checklist

- Clarification complete.
- Acceptance notes updated.
- Architecture reviewed.
- Test and validation plan defined.
- UX and workflow reviewed.
- Non-goals confirmed.
- Ready for build.

## Clarification Notes

- On 2026-07-06, the user clarified that the existing fixed-size Circle tool
  should become Brush, and a separate Circle shape should work like Rectangle by
  stretching during drag.
- Implementation resolves the drag geometry as center-at-start with radius to
  the current pointer. The radius is clamped to the existing persisted circle
  contract of 100 percent diameter, or normalized radius `0.5`, against the
  shorter map side.

## Harness Compliance

Harness Version: 1

## Review Scope Checklist

| Area | Applicability | Evidence |
| --- | --- | --- |
| Persistence/data integrity | Reviewed | Proposed implementation should reuse existing `hide-circle` and `reveal-circle` operation persistence without schema migration. |
| External inputs | Reviewed | Existing GM-only fog operation route validation accepts bounded normalized circle payloads. |
| Security/trust boundaries | Reviewed | Circle commits must remain GM-only and Player Display must stay read-only. |
| Error handling/observability | Reviewed | Invalid or tiny drafts should not mutate state; route validation failures should leave fog unchanged. |
| Module boundaries | Reviewed | GM input should derive drag-sized circle geometry; server, storage, projection, and renderer should reuse existing circle operation handling. |
| Dependencies | Not Applicable | No dependency changes planned. |
| Browser behavior | Reviewed | Chromium coverage should exercise drag preview, tiny-drag rejection, Escape cancel, hide/reveal, shown sync, and unshown isolation. |
| Test realism | Reviewed | Tests should use real uploaded maps, pointer events, persisted campaign JSON, and player canvas assertions. |
| Resource cleanup | Reviewed | Existing browser and server fixtures should clean temporary campaign roots, sockets, and browser contexts. |

## SDLC Tier

Tier: 3.

Rationale: Drag-to-size Circle changes GM fog input and map/fog rendering
semantics for persisted fog operations that may sync to the Player Display.
Those areas are Tier 3 under
`docs/decisions/decision-004-mandatory-subagent-sdlc.md`.

## SDLC Evidence

### Clarification

- Reviewer: /root
- Completed: 2026-07-06
- Result: Pass
- Evidence: User approved splitting the work into a Brush correction and a
  separate real Circle shape that works like Rectangle.

### Architecture Review

- Reviewer: /root/f008e_planning_review
- Completed: 2026-07-06
- Result: Pass
- Evidence: Recommended explicit three-shape dispatch for Rectangle, Brush,
  and Circle; preserving the existing `hide-circle` and `reveal-circle`
  operation schema; clamping drag-sized Circle radius to the current server
  contract; adding a distinct Circle button; and testing one-operation Circle
  commits plus preview, cancel, shown/unshown, and Player Display boundaries.

### Implementation Design

- Reviewer: /root/f008e_planning_review
- Completed: 2026-07-06
- Result: Pass
- Evidence: Implementation should add a separate Circle UI shape, route Circle
  pointer events independently from Rectangle and Brush, calculate drag radius
  client-side from the start point to the current pointer, clamp to normalized
  radius `0.5`, and reuse the existing GM-only fog append API and renderer.

### Test and Validation Design

- Reviewer: /root/f008e_planning_review
- Completed: 2026-07-06
- Result: Pass
- Evidence: Add Chromium coverage proving Circle preview, tiny-drag rejection,
  Escape cancel, one-operation hide and reveal commits, persisted operation
  shape and radius, Brush compatibility, unshown isolation, and Player Display
  read-only behavior.

### UX and Workflow Review

- Reviewer: /root/f008e_planning_review
- Completed: 2026-07-06
- Result: Pass
- Evidence: Recommended a distinct Circle control alongside Rectangle and
  Brush, keeping Brush diameter visible only for Brush and making the Circle
  drag behavior visually explicit with the existing circular draft preview.

### Coding and TDD

- Reviewer: /root
- Completed: 2026-07-06
- Result: Pass
- Evidence: Added a separate Circle control, explicit Rectangle/Brush/Circle
  pointer dispatch, drag-sized Circle preview and commit paths, renderer-owned
  drag-to-circle geometry, Brush-only diameter controls, and Chromium coverage
  for tiny-drag rejection, hide/reveal commits, edge-radius clamping, Escape
  cancel, persistence, shown-player sync, unshown isolation, and Brush
  compatibility.

### Code Review

- Reviewer: /root/f008e_code_review
- Completed: 2026-07-06
- Result: Pass
- Evidence: Reviewed Circle UI state, GM pointer flow, controller commit path,
  renderer geometry, persistence shape, and browser coverage. Found that radius
  should be computed from the raw pointer delta before clamping to preserve
  intended edge drags; this was fixed with raw end coordinates and an edge
  clamp regression assertion. Also noted docs/tracker state was still pending
  completion updates.

### Docs and Tracker Review

- Reviewer: /root/f008e_docs_tracker_review
- Completed: 2026-07-06
- Result: Pass
- Evidence: Reviewed F-008E, the feature tracker, F-008 umbrella, roadmap,
  README, and acceptance tests. Found stale planned/proposed status in F-008E,
  tracker, roadmap, umbrella, and README current-status summary; those were
  updated to reflect completed F-008E validation. Acceptance-test coverage was
  found current.

### Final-Diff Review

- Reviewer: /root/f008e_final_diff_review
- Completed: 2026-07-06
- Result: Pass
- Evidence: Reviewed runtime, tests, docs, tracker, and baseline diff. Found
  stale lifecycle status in docs and tracker, fixed during docs review. Also
  noted Circle browser coverage should assert preview geometry, not only draft
  visibility and persisted commit geometry; a bounding-box assertion was added
  for the circular draft center and diameter.

### Final Validation

- Reviewer: /root
- Completed: 2026-07-06
- Result: Pass
- Evidence: Focused checks passed: `npm run format:check`,
  `node --test test/state.test.js test/campaign-storage.test.js
  test/server.test.js`, `npx playwright test browser-test/workflows.spec.js
  --grep "circle fog tool"`, `npx playwright test
  browser-test/workflows.spec.js --grep "brush fog tool|circle fog tool"`, and
  `npm run modules:check`. Final `npm run harness:check` and
  `npm run quality` passed; quality included lint, format, module baseline,
  harness compliance, Node coverage, 31 Chromium workflow tests, and
  high-severity dependency audit.

## Material Findings

| ID | Stage | Reviewer | Severity | Finding | Recommendation | Disposition | Evidence | Approved By | Follow-up | Review By | Residual Risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F-008E-R001 | Architecture Review | /root/f008e_planning_review | High | Shape dispatch is currently binary, so a naive Circle addition would be treated as Rectangle. | Model Rectangle, Brush, and Circle as explicit UI shapes and dispatch each pointer flow separately. | Fixed | Planned implementation uses three explicit UI shapes and separate Circle commit/preview actions. | N/A | N/A | N/A | Low. |
| F-008E-R002 | Architecture Review | /root/f008e_planning_review | High | Drag-sized Circle can exceed the existing server radius limit and fail on commit. | Clamp Circle radius to the current normalized `0.5` server contract or update validation deliberately. | Fixed | F-008E resolves Circle drag radius as clamped to normalized `0.5`, preserving existing validation. | N/A | N/A | N/A | Low. |
| F-008E-R003 | Implementation Design | /root/f008e_planning_review | Medium | Circle drag geometry was unresolved. | Resolve and document the geometry before implementation. | Fixed | F-008E uses center-at-start with radius-to-pointer, clamped to the existing 100 percent diameter contract. | N/A | N/A | N/A | Low. |
| F-008E-R004 | UX and Workflow Review | /root/f008e_planning_review | Medium | Brush and Circle need separate controls so Brush diameter does not apply to Circle. | Add a distinct Circle button and show Brush diameter only for Brush. | Fixed | Planned UI has Rectangle, Brush, and Circle controls, with diameter visible only for Brush. | N/A | N/A | N/A | Low. |
| F-008E-R005 | Code Review | /root/f008e_code_review | Medium | Circle radius calculation used the clamped end coordinate, so dragging beyond a map edge would under-size the intended clamped circle. | Calculate radius from the raw pointer delta, then clamp only the final radius to normalized `0.5`. | Fixed | `getNormalizedCircleFromClientPoints` now keeps raw end coordinates for the radius calculation, and the Circle browser workflow asserts an outward edge drag persists radius `0.5`. | N/A | N/A | N/A | Low. |
| F-008E-R006 | Code Review | /root/f008e_code_review | Low | Docs and tracker status were still planned while implementation was in progress. | Update feature evidence, tracker, roadmap, acceptance tests, and README before completion. | Fixed | F-008E evidence, tracker, roadmap, README, and acceptance tests were updated before final validation. | N/A | N/A | N/A | Low. |
| F-008E-R007 | Docs and Tracker Review | /root/f008e_docs_tracker_review | High | F-008E docs, tracker, roadmap, F-008 umbrella, and README status summary still described the implemented work as planned, proposed, or validated only through F-008D. | Update lifecycle/status text and completion evidence before final validation. | Fixed | F-008E is Done; tracker marks F-008E Complete/Done; roadmap and F-008 umbrella list drag-to-size Circle as Complete; README status says validation through F-008E. | N/A | N/A | N/A | Low. |
| F-008E-R008 | Final-Diff Review | /root/f008e_final_diff_review | Low | Circle browser coverage verified draft visibility and persisted commit geometry but not preview geometry. | Add a bounding-box assertion that the draft circle is centered on drag start and has the expected diameter during preview. | Fixed | Circle workflow now checks `#workspace-fog-circle` center and width/height against the drag start and pointer radius before Escape cancellation. | N/A | N/A | N/A | Low. |

## Affected Systems

- GM view: adds a separate drag-to-size Circle shape choice.
- Player view: receives circle fog only for the encounter currently
  `Shown to Players`.
- Local server: expected to reuse existing circle operation validation.
- Map/fog rendering: previews and commits drag-sized circular fog.
- Storage: expected to reuse existing circle operation persistence.
- Documentation: tracker, README, and acceptance tests should describe Circle
  separately from Brush when implemented.

## Validation Plan

Focused Chromium coverage validates drag-to-size Circle hide and reveal
behavior, tiny-drag rejection, Escape cancel, shown/unshown sync boundaries,
persistence, edge-radius clamping, Brush compatibility, and responsive
controls. Completion validation includes focused browser tests, relevant Node
fog tests, `npm run harness:check`, and `npm run quality`.

## Open Questions

- Resolved: Circle drag uses center-at-start with radius to current pointer,
  clamped to normalized radius `0.5`.

## Status

Done.
